const LandingPageLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      {/* Header */}
      <main>{children}</main>
      {/* Footer */}
    </>
  );
};

export default LandingPageLayout;
